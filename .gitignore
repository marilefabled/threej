# deps / build
node_modules/
dist/

# macOS
.DS_Store

# Editor / local
*.log
.claude/settings.local.json

# Extracted Unity assets (can be huge) + raw asset dumps. Never commit purchased
# vendor assets to the repo.
extracted/
public/vendor/
*.unitypackage
